package {{ .Conf.RootPackage }}

// GENERATED BY CODEGEN.
	
// Constants for table columns. 
const (
{{ $iotaSet := false -}}
{{- range $i, $s := .Conf.Schemas }}
	{{- range $j, $t := $s.Tables }}
		{{- if $t.Generate }}
			// Columns for table {{ $s.Name }}.{{ $t.Title }}
			{{- range $k, $f := $t.Fields }}
				{{- if $iotaSet }}
					{{ $t.Title }}_{{ $f.Title }}
				{{- else }}
					{{ $t.Title }}_{{ $f.Title }} = iota
					{{- $iotaSet = true -}}
				{{- end -}}
			{{ end }}
		{{ end -}}
	{{ end }}
{{ end }}
)

 // ^^ END OF GENERATED BY CODEGEN. ^^
